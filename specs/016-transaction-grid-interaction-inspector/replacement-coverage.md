# Replacement Coverage Map

This is the exhaustive superseded-path inventory for current HEAD
`3bc789cee63d85d966c7c395e73f1bcd0bad04be`, derived by sweeping transaction production, unit, E2E,
and performance artifacts for navigation, live editors, cell selection, focus, notes expansion,
dynamic measurement, rule popovers, and presence ownership. Re-run the same sweep before deletion;
any newly reachable path must be added and independently reviewed.

No predecessor may be deleted or rewritten merely because it asserts superseded mechanics. First add
the named replacement oracle, neutralize or invert the production mechanism so that oracle fails,
then restore and obtain independent review. Preserve tests may change locators/surface only while
retaining their user assertion.

## Production component and hook inventory

| Current production path                                                                                                                                          | Disposition                                                 | Replacement oracle required before change/deletion                                                                                                                 | Negative proof                                                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| `src/app/(app)/transactions/page.tsx` table ownership, focus-description props, rule/proposal render callbacks                                                   | Replace integration boundary                                | Workspace owns controller/selection/inspector; Add reveals Description full edit once; page filters/toolbars do not rerender on drag; rule state reaches inspector | Bypass workspace or reattach table to page and show rerender/focus/inspector tests fail                                                 |
| `TransactionTable.tsx` `focusedId`, `expandedIds`, `useGridCellNavigation`, key/copy handlers, local selection integration, focus/add pins, robot/proposal props | Replace controller and DOM ownership                        | One canonical external atom; typed engagement; one roving tab stop; active/pending pins; atomic generation/focus/reconciliation; no inline notes/robot geometry    | Reintroduce a second owner, clear the canonical one-cell reconcile result, or restore `focusedId`; targeted state/focus tests must fail |
| `TransactionRow.tsx` always-live controls, row-local edit flags, selected-marker reads, expand action, notes textarea/ARIA row, robot/proposal wrappers          | Replace renderer                                            | Display-first `TransactionGridCell`; shared editor adapters; selectable actions cell; no notes row; fixed 57px; stable owner markers                               | Mount live controls or notes row, or bypass controller draft; structure, focus, height, and cancel tests fail                           |
| `TransactionVirtualRows.tsx` dynamic `measureElement`, expanded-note height comments/branches                                                                    | Replace measurement; preserve leaf boundary                 | Exact 57px rendered rows, full count, bounded held model, active/pending pins, stable keys, compiler-skipped virtualizer leaf                                      | Restore dynamic measurement or make a state grow the row; exact-height and mounted-bound tests fail                                     |
| `hooks/useGridCellNavigation.ts` and `hooks/index.ts` export                                                                                                     | Delete after controller coverage                            | Pure mode/key reducer plus registration/focus adapter covers every command, notes are absent, stable IDs not DOM row scans                                         | Restore hook or route one arrow through it; single-owner and command-matrix tests fail                                                  |
| `table-model/cell-key-intent.ts` and its `table-model/index.ts` exports                                                                                          | Delete/replace                                              | Acceptance-keyed mode × key × modifier × IME reducer, typed results, popup/inspector precedence                                                                    | Invert each reducer branch and prove its paired matrix case fails                                                                       |
| `table-model/features.ts` editable-only metadata and cell-selection registration                                                                                 | Extend                                                      | Declarative focus/select/copy/edit/activation/popup/automation capabilities and explicit drag/reset options                                                        | Remove one capability or flip drag/reset option; column/gesture tests fail                                                              |
| `table-model/ids.ts` missing actions marker and notes marker contract                                                                                            | Replace actions/notes clauses; preserve brands              | Real stable `actions` cell marker; notes removed from grid identity; branded projection generation/address types                                                   | Restore orphan actions/null marker or notes cell identity; columns/ARIA tests fail                                                      |
| `table-model/columns.ts` notes/actions exclusions and copy metadata                                                                                              | Extend/replace                                              | Actions selectable/non-copyable with empty TSV field; notes absent; all capabilities complete for dynamic allocations                                              | Make actions unselectable/copyable or add notes column; selection/copy tests fail                                                       |
| `table-model/matching-set.ts` reset-on-matching-set behavior and options                                                                                         | Replace reset authority; preserve row matching              | Structural generation creates atomic one-cell/idle result; value/window changes retain state; row selection stays independent                                      | Clear selection without replacement or reset on held-window move; reconciliation tests fail                                             |
| `table-model/cell-selection-render.ts` mounted sparse geometry                                                                                                   | Preserve subscription boundary; replace geometry source     | Projection-coordinate membership/edges, muted/parked visibility, no whole-range enumeration                                                                        | Use sparse model indexes or enumerate range in render; offscreen/complexity tests fail                                                  |
| `table-model/clipboard.ts` and `copy-intent.ts`                                                                                                                  | Preserve formatters; replace ownership/limits               | Latest inclusive operation only, exclusion no-op, empty actions/checkbox positions, generation and size failure atomicity                                          | Choose an earlier include, partially write, or intercept native editor copy; copy tests fail                                            |
| `row-window.ts` one-pin helper                                                                                                                                   | Extend                                                      | Active/editor plus pending-reveal pins, sparse pins never neighbors, same-generation rollback/G+1 discard                                                          | Treat sparse list adjacency as logical or restore stale pin; property tests fail                                                        |
| `cursor-row-order.ts` row-selection lazy order                                                                                                                   | Preserve row-selection use; do not reuse as cell projection | Complete-matching row checkbox ranges remain lazy; cell adapter uses generation-checked `indexOf/idAt/readRowAt/rowsBetween`                                       | Route cell navigation through held/lazy row-selection order; generation/non-adjacency tests fail                                        |
| `transaction-reveal-intent.ts` focus-description consume-once intent                                                                                             | Adapt and preserve once-only assertion                      | Stable Add intent becomes materialize → active Description → full edit once through controller                                                                     | Reapply retired focus or transfer by index; Add once-only tests fail                                                                    |
| `cells/InlineEditableAmount.tsx`                                                                                                                                 | Migrate                                                     | Typed amount draft; invalid syntax never becomes zero; commit once; cancel; fixed box                                                                              | Bypass validator or double commit; validation/undo tests fail                                                                           |
| `cells/PersonAllocationCell.tsx`                                                                                                                                 | Migrate                                                     | Existing allocation validation/effective description plus shared draft lifecycle and fixed box                                                                     | Commit invalid/out-of-range or grow row; allocation tests fail                                                                          |
| `cells/InlineEditableDate.tsx`                                                                                                                                   | Migrate                                                     | Full text edit, Tab-to-DayPicker, popup outside-pointer order, locale parsing, Escape layers                                                                       | Make Enter open calendar or allow invalid outside click; date focus/E2E tests fail                                                      |
| `features/accounts/AccountCombobox.tsx` and account cell integration                                                                                             | Migrate adapter, preserve Create Account dialog             | Quick exact resolution; full popup/create; controller draft; owner metadata; cancel/focus return                                                                   | Write on option click or create from quick text; picker tests fail                                                                      |
| `cells/InlineEditableStatus.tsx`                                                                                                                                 | Migrate                                                     | Exact quick resolution, controller draft, popup ownership, no immediate write                                                                                      | Restore immediate `onValueChange`; cancel/undo tests fail                                                                               |
| `cells/InlineEditableTags.tsx`                                                                                                                                   | Migrate                                                     | Draft-only toggles; atomic create-and-assign quick command; outside-pointer order; Escape true revert                                                              | Split tag create/assignment or write each toggle; atomicity/cancel/automation tests fail                                                |
| `cells/InlineEditableDescriptionAlias.tsx`                                                                                                                       | Migrate last                                                | Quick/full draft, lazy autocomplete, modal precedence, alias commands, one-shot Add focus, owner metadata                                                          | Let blur race alias selection or write before modal; alias tests fail                                                                   |
| `cells/CheckboxCell.tsx` and row action controls                                                                                                                 | Migrate activation surface                                  | Background selects cell; glyph activates row selection; actions primary opens inspector; duplicate/delete remain explicit                                          | Activate on navigation or let background toggle; gesture/action tests fail                                                              |
| `TransactionRuleProposal.tsx` popover anchor and document focus listeners                                                                                        | Replace geometry/effect owner                               | Headless controller uses transaction-owner predicate; inspector section preserves modes/auto-apply                                                                 | Restore near-cell popover or row-only focus exit; owner/geometry tests fail                                                             |
| `TransactionRuleRobot.tsx` popover/auto-open geometry                                                                                                            | Replace surface; preserve rule state                        | Inspector existing-rule/drift section reuses `FieldRuleEditor`; no row robot                                                                                       | Render robot or occluding popup; no-row-automation and parity tests fail                                                                |
| `TransactionRulePopup.tsx`                                                                                                                                       | Replace wrapper; reuse editor                               | Inspector hosts same editor and apply commands with stable return focus                                                                                            | Substitute divergent inspector editor; parity tests fail                                                                                |
| `use-field-rule-proposal.ts`, `use-transaction-rule-workflow.ts`, `field-rule-*-state.ts`, `description-rule-state.ts`, `FieldRuleProposal.tsx`                  | Preserve/extract headless logic                             | Controllers remain mounted, draft context immediate, changed commit only, pending state keyed by transaction+field                                                 | Unmount on panel close or update proposal on unchanged/invalid commit; state tests fail                                                 |
| `src/lib/crdt/schema.ts`, `display-preferences.ts`, and display-preference context hooks                                                                         | Extend only                                                 | Optional viewer-scoped inspector field, default open, field-level merge with date format, LWW same-field                                                           | Replace whole record or share viewer state; CRDT preference tests fail                                                                  |

## Unit, DOM, and integration test inventory

| Current test path/assertion                                                                                              | Disposition                                                              | Replacement oracle and required negative proof                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/unit/transactions/table-model/cell-key-intent.test.ts` caret boundaries                                           | Replace                                                                  | Acceptance-keyed command and IME matrix; invert every decision branch                                                                                         |
| `cell-selection-gestures.test.tsx` focused-input anchor, caret-edge extension, no drag, excluded controls, clear-on-blur | Replace mechanics/extend                                                 | Click/double/Shift/modifier/drag/autoscroll, anchor versus extent, direct activation, inspector muted/parked, one roving tab stop; flip each gesture owner    |
| `table-model/cell-selection.test.ts` mounted Table geometry                                                              | Preserve real TanStack fixture, replace canonical use                    | Arbitrary/disjoint/offscreen ranges and atomic one-cell reconciliation; force sparse-gap compression                                                          |
| `table-model/cell-selection-render.test.ts`                                                                              | Preserve/extend                                                          | Projection-coordinate edges and visibility with row-local subscription; replace with sparse getter to fail                                                    |
| `table-model/react-binding.test.tsx` internal cell-selection state/identity assertions                                   | Replace cell-selection cases, preserve compiler premise for other slices | External atom updates render subscribed cells without requiring a second table-owned state identity; mutate to internal `setFocusedCell` ownership and fail   |
| `table-model/columns.test.ts` actions unmarked and notes/actions outside model                                           | Replace those assertions; preserve order/accessors/template              | Assert real actions marker/capabilities, notes absent, non-copyable empty shape; restore null actions marker or notes marker and fail                         |
| `table-model/clipboard.test.ts` and `copy-intent.test.ts`, including textarea-notes/native caret assumptions             | Preserve formatter/native precedence, replace notes/single-cell rule     | Latest active include, exclusion no-op, inspector/native editor ownership, numeric/byte limits, stale failure; partial-write and earlier-range mutations fail |
| `row-window.test.ts`, `cursor-row-order.test.ts`, cursor differential/performance tests                                  | Preserve/extend                                                          | Two pins, non-adjacency, generation adapter, existing `indexOf`; stale-pin/logical-neighbor mutations fail                                                    |
| `add-transaction-focus*.test.tsx`                                                                                        | Preserve assertion, replace mechanism                                    | Row selection retained; materialize; one-cell active Description; full edit once; stale generation loses to reconcile; replay request mutation fails          |
| `allocation-grid.test.tsx` expanded-notes shared-template assertion                                                      | Replace that assertion; preserve allocation model/validation             | Row and header/add share columns, inspector owns notes, one 57px row; restore notes row or omit allocation track and fail                                     |
| `date-cell-locale-entry.test.tsx` and date-format preference tests                                                       | Preserve/extend                                                          | Locale parse/display plus date popup, invalid outside-pointer, and focus ordering; transpose parse or allow invalid leave and fail                            |
| `cell-hit-area.test.ts` and `cell-resting-chrome.test.ts`                                                                | Replace activation mechanics, preserve geometry                          | Background selects, direct glyph activates, double-click edits, unchanged resting dimensions; reapply overlay activation and fail                             |
| `virtualization.test.tsx` dynamic first-render measurement/focused pin/template cases                                    | Replace dynamic measurement/focus mechanism; preserve count/window       | Exact 57px DOM height, active/pending pins, deep reveal, template; change rendered height or restore measureElement and fail                                  |
| `tests/unit/transactions/virtual-grid-harness.ts` real TanStack Virtual jsdom adapter                                    | Preserve fixed-row stand-ins; retire variable measurement                | Fixed-height real-virtualizer, deep-scroll, and bounded-window oracles; restore variable measurement/observer code or a fake virtualizer and fail             |
| `full-count-virtualization.test.tsx` focused row pin                                                                     | Replace focus source, preserve full count/bound                          | Controller active ID pins; pending reveal second pin; selection endpoints do not mount; pin every selected endpoint and fail bound                            |
| `description-alias-interactions.test.tsx`                                                                                | Preserve domain, replace lifecycle                                       | Quick/full/modal/popup outside pointer/IME/focus return; restore blur race and fail                                                                           |
| field-rule proposal/robot/auto-apply/stability unit tests                                                                | Preserve business assertions, replace geometry/owner predicate           | Headless pending state, inspector close/open independence, row+inspector+portal owner exit; restore row-only predicate and fail                               |
| display-preference/defaults/CRDT integration tests                                                                       | Extend                                                                   | Absent open, field-preserving write, concurrent different-field merge, same-field LWW, viewer isolation, remote close focus; whole-record replacement fails   |
| allocation, mutation, undo, import-rule, sync, and automation integration suites                                         | Preserve/extend                                                          | One editor commit/atomic tag create is one Loro action and one automation transition; split or duplicate mutation fails                                       |

### Virtual-grid harness support boundary

`tests/unit/transactions/virtual-grid-harness.ts` keeps the real TanStack Virtual adapter, nonzero
jsdom viewport/scroll geometry, asynchronous browser-like scroll delivery, absolute virtual indexes,
contiguous row windows, real `insertTransaction` store setup, fixed 57px arithmetic, viewport, and
overscan. After production removes `measureElement`, retire `measuredRowHeight`,
`onProgrammaticScroll`, its measuring `ResizeObserver`, 57/75/103px remeasurement, scroll
adjustment, and any separately synchronized row-height constant. Restoring those retired paths or
replacing the real virtualizer with a fake must fail the fixed-height, deep-scroll, and
bounded-window oracles.

Its complete immediate import boundary is `@testing-library/react`, `row-window.ts`, `mutations.ts`,
and `schema.ts`. Its direct test consumers are `add-transaction-focus.test.tsx`,
`add-transaction-focus-once.test.tsx`, `cell-selection-gestures.test.tsx`,
`virtualization.test.tsx`, `full-count-virtualization.test.tsx`,
`historical-allocation-column-window.test.tsx`, `select-all-beyond-page.test.tsx`,
`search-alias-resolved-description.test.tsx`, and `transactions-page-harness.tsx`. Preserve their
existing domain assertions where listed above: one-shot Add activation, complete matching
count/selection, historical allocation columns, alias/notes search, real store construction, and
bounded absolute-window behavior.

## E2E inventory

| Current suite/journey                                                                                             | Disposition                                  | Replacement coverage and negative proof                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `transactions.spec.ts` Add ordinary rows/focus                                                                    | Preserve result                              | Controller materialization/full edit once, row selection unchanged; stale/replay mutation fails                                                                       |
| large-list focus/edit/filter/navigation                                                                           | Preserve/extend                              | Cross-window move/extend/copy, G reveal rejected after G+1 filter, bounded DOM; stale restore mutation fails                                                          |
| T012/T013 click/Enter/Escape/Tab                                                                                  | Replace mechanics                            | Parked → visible → full edit, commit/cancel layers, internal stops, boundary exit; old live-input behavior fails                                                      |
| T014a and “Enter opens date calendar”                                                                             | Replace                                      | Enter full text edit, Tab calendar, popup ordering, invalid blocking; Enter-calendar mutation fails                                                                   |
| T015/T016/T016a/T017 amount/status/account/tags live controls                                                     | Replace                                      | Display-first quick/full drafts, exact/unresolved search, atomic tag create, owner/focus/cancel; immediate-write mutation fails                                       |
| tag creation journeys                                                                                             | Preserve result/replace transaction boundary | Explicit atomic create-and-assign quick choice and full-edit create; split-action fault fails                                                                         |
| arrow/caret-boundary and description-to-notes navigation                                                          | Replace                                      | Quick grid arrows, full native horizontal caret, Shift extent, Alt movement, notes absent from grid; old hook route fails                                             |
| T020/T020b and T021a–T021g row checkbox/body/select-all                                                           | Preserve and add cell selection              | Glyph row selection versus background cell selection; complete-matching baseline independent; cross-domain mutation fails                                             |
| T026–T028b bulk edits                                                                                             | Preserve                                     | Toolbar targets row selection only; grid ranges never bulk target; coupling mutation fails                                                                            |
| T037–T040 notes row/edit/expand/search                                                                            | Replace display/edit/expand; preserve search | Inspector notes, immediate encrypted persistence, no second row, active-row follow, notes search; inline-row mutation fails                                           |
| UR-005 resting chrome and UR-012 edge activation                                                                  | Preserve geometry, replace activation        | Display-first/fixed height/non-layout paint/direct control; overlay or row-growth mutation fails                                                                      |
| `transaction-grid-structure.spec.ts`                                                                              | Replace/extend                               | Grid/row/gridcell counts/indexes, activation cells, one tab stop, parked/visible/muted, editor/portal/inspector labels, no notes row; duplicate aside/notes row fails |
| `description-aliases.spec.ts`                                                                                     | Preserve                                     | Alias creation/rename/modal and popup/outside focus through shared editor; blur-race mutation fails                                                                   |
| `rule-creation-controls.spec.ts`, `transaction-rules.spec.ts`, `automations.spec.ts`, `field-rule-parity.spec.ts` | Preserve rules, replace geometry             | Same modes/restrictions/precedence/apply/parity in inspector; row robot/near-cell popup fails                                                                         |
| `presence.spec.ts`                                                                                                | Preserve identity semantics                  | Navigating/parked/inspecting/owned popup viewing and editing field only; destination/value publication fails                                                          |
| `undo-redo.spec.ts`                                                                                               | Preserve/extend                              | One changed commit or atomic tag create is one undo item; cancel/invalid/toggle drafts add none; split action fails                                                   |
| `tab-duplication.spec.ts`, persistence/sync/reload suites                                                         | Preserve/extend                              | Selection/draft/panel preference converge without lost writes or duplicate owners; whole-record preference mutation fails                                             |
| allocation, settlement, import, locale, tags, filtering, bulk-selection suites                                    | Preserve                                     | No interaction rewrite changes financial values, mutation scope, search, import, locale, or complete-set row selection                                                |

## Performance harness inventory

| Current artifact                                                                                                                  | Disposition                               | Replacement oracle and negative proof                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/perf/measure-grid.ts` `NotesOutcome`, `applyNotesAndExpand`, 2-ARIA-row/taller-row assertions, `notesCap`, geometry reread | Replace                                   | Inspector notes, one 57px row, interaction/responsive identity; restore taller-row assertion, second row, or geometry reread and fail fixture gate                                |
| `tests/perf/baseline.measure.ts` standalone campaign entrypoint                                                                   | Replace note arm and evidence destination | Preserve campaign rigor; retire expanded-note fields; require revisioned source 016 output. Restore spec 015 output or note geometry and fail evidence/provenance/fixed-row gates |
| `tests/perf/grid-sampler.ts` stable row/viewport semantic sampling                                                                | Preserve/extend                           | Sample fixed row geometry and inspector interaction without treating inspector as row coverage; omit row band or duplicate stable ID and fail classifier                          |
| `tests/perf/frame-report.ts`, `run-statistics.ts`, and tests                                                                      | Preserve classifiers/statistics           | Retain full/blank/partial/stale/dropped classifiers, per-run verdict, and within/between/pooled variance; neutralize a classifier or admit one failed run and fail                |
| `tests/perf/fixture/transaction-fixture.ts`                                                                                       | Preserve data; retire geometry plan       | Preserve deterministic fixture/digests and note data for inspector seeding; reuse `notedRows` to drive variable row height and fail the fixed-row fixture gate                    |
| `tests/perf/harness-digest.ts`, `scroll-routes.ts`, `vitest.measure.config.ts`, trace extraction                                  | Preserve/extend support boundary          | Preserve provenance, route identity, explicit discovery, and tree attestation; omit immediate support, restore stale variable-row offsets, or permit drift and fail               |
| spec 015 dynamic-note captures                                                                                                    | Historical only, never overwrite          | Fixed-row plus inspector captures write only to revisioned source 016 evidence; an old capture or location cannot satisfy a source 016 key                                        |

### Baseline campaign support boundary

`tests/perf/baseline.measure.ts` keeps standalone Node/Vitest execution, artifact writes after every
repeat and in `finally`, silent-exit detection, provenance/fixture/harness digests,
clean/instrumented route serialization, session/repeat accounting, per-run and variance tables, and
every-route/every-run thresholds. Retire `PERF_NOTES_ROWS`, `notesCap`, `notesPlanned`,
`measurement.notes`, expanded ARIA-row/heights, and expanded/collapsed note lines. Before relevant
deletion or any source 016 campaign, its default output must be a new immutable revision such as
`specs/016-transaction-grid-interaction-inspector/evidence/measurements/performance-01/`, never the
historical spec 015 location. Restoring the old location or any expanded-note geometry must fail the
evidence-location, provenance, and fixed-row gates.

Its complete immediate import boundary is `fixture/transaction-fixture.ts`, `frame-report.ts`,
`harness-digest.ts`, `measure-grid.ts`, `run-statistics.ts`, `scroll-routes.ts`, Node fs/path, and
Vitest; `vitest.measure.config.ts` remains its direct discovery boundary. Preserve deterministic
10,000-row account/person/allocation/tag/rule data and digests, note values as inspector seed data,
frame classifiers, run statistics, start-of-process digest capture, HTTPS/quiet-host/stimulus
fidelity, wall-clock route identities, and measure-test discovery. Retire `notedRows` as a
variable-height plan, rederive geometry-dependent route offsets for fixed 57px rows, update digest
inventories for source 016 scenarios, and make omission of any immediate support file fail
provenance.

## Deletion gate

Before deleting any superseded path:

1. repeat the source/test/perf sweep and append newly discovered paths;
2. cite its replacement acceptance key, automated oracle, fault mutation, and independent review;
3. prove no remaining import/export or test names the old mechanism;
4. preserve all unaffected financial, CRDT, row-selection, accessibility, and performance
   assertions;
5. record deletion in `evidence/implementation/superseded-code-01.md`.
