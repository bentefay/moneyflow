# P30 — UR-009

- **Status:** queued
- **Requirement:** `UR-009` (frozen source `specs/011-automations-conformance/spec.md`, markerless)
- **Requirement task:** [`ur-009.md`](ur-009.md)
- **Implementer:** `human_scratch_implementer`
- **Independent gate:** DISTINCT `human_scratch_reviewer`, never the implementer
- **Allowed implementer write:** product + tests + `evidence/P30/implementation-<NN>.md`
- **Allowed reviewer write:** `reviews/P30-review-<NN>.md` only
- **Ledger/marker writes:** root coordinator only

## Contract

Re-verify automations against the principal's frozen wording at `specs/human-scratch.md:248-295` and
fix every divergence. See [`ur-009.md`](ur-009.md). Rule from the frozen text.

`HS-007` and `P17A`-`P17D` remain `passed` and are NOT reopened; this package is an independent
conformance pass over the same frozen clauses.

Repo hard rules: no `as`, no `any`, no `!` in product code; functional and immutable; tests
mandatory; never Playwright `--debug`/`--ui`/`--headed`/`show`; no parentheses in commit messages.

All six checks pass before handback, E2E as FULL-SUITE `--retries=0`. Next 16's dev lock is
distDir-scoped and a human dev server may hold the main checkout: run E2E in an isolated git
worktree and never kill that server.

Secret-safety is blocking. Never commit the principal's real financial data; use synthetic fixtures.

## Completion

`UR-009` is markerless and immutable. It passes only when P30 passes independent review.
