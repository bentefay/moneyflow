# P22 — UR-001

- **Status:** queued
- **Requirement:** `UR-001` (frozen source `specs/009-user-reported-refinements/spec.md`,
  markerless)
- **Requirement task:** [`ur-001.md`](ur-001.md)
- **Implementer:** `human_scratch_implementer` (full product/test implementation)
- **Independent gate:** DISTINCT `human_scratch_reviewer`, never the implementer
- **Allowed implementer write:** product + tests + the exact dispatched
  `evidence/P22/implementation-<NN>.md`
- **Allowed reviewer write:** the exact dispatched `reviews/P22-review-<NN>.md` only
- **Ledger/marker writes:** root coordinator only

## Contract

Implement UR-001 exactly as its frozen source requires; see [`ur-001.md`](ur-001.md) for the
required behaviour and orientation. Rule from the frozen text, not from orientation notes.

Repo hard rules apply: no `as`, no `any`, no `!` in product code; functional and immutable style;
money as integer minor units; tests are mandatory (unit for pure functions, E2E for user flows);
never run Playwright with `--debug`, `--ui`, `--headed` or `show`; no parentheses in commit
messages.

All six checks must pass before handback:
`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm test:e2e`. E2E is validated
with FULL-SUITE `--retries=0` runs per the documented load-dependent flake discipline, never in
isolation.

Secret-safety is blocking: no vault master key, seed phrase, recovery material, `crypto_box` secret,
`SUPABASE_JWT_SECRET` value, presence key, invite fragment or vault plaintext in code, logs,
fixtures or evidence. Tests use public or synthetic vectors only.

## Completion

UR-001 is markerless and immutable: there is no checkbox and no source edit. It passes only when P22
passes its independent review, recorded in the requirement and package ledgers by root.
