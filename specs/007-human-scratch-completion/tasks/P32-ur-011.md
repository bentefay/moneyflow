# P32 — UR-011

- **Status:** queued
- **Requirement:** `UR-011` (frozen source `specs/012-transaction-selection/spec.md`, markerless)
- **Requirement task:** [`ur-011.md`](ur-011.md)
- **Implementer:** `human_scratch_implementer`
- **Independent gate:** DISTINCT `human_scratch_reviewer`, never the implementer
- **Allowed implementer write:** product + tests + `evidence/P32/implementation-<NN>.md`
- **Allowed reviewer write:** `reviews/P32-review-<NN>.md` only
- **Ledger/marker writes:** root coordinator only

## Contract

Implement UR-011 exactly as its frozen source requires; see [`ur-011.md`](ur-011.md). Rule from the
frozen text.

Repo hard rules: no `as`, no `any`, no `!` in product code; functional and immutable; tests
mandatory; never Playwright `--debug`/`--ui`/`--headed`/`show`; no parentheses in commit messages.

All six checks pass before handback, E2E as FULL-SUITE `--retries=0`. Next 16's dev lock is
distDir-scoped and `playwright.config.ts` pins :3000 with no env override, so exactly ONE campaign
runs repo-wide: use your own git worktree and ask root to sequence the port. Never `CI=true` for the
runs — it yields 1 worker and 2 retries, inverting the required profile.

Secret-safety is blocking. Never commit the principal's real financial data; use synthetic fixtures.

## Completion

UR-011 is markerless and immutable. It passes only when P32 passes independent review.
