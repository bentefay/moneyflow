# P33 — UR-012

- **Status:** queued
- **Requirement:** `UR-012` (frozen source `specs/013-transaction-cell-hit-area/spec.md`,
  markerless)
- **Requirement task:** [`ur-012.md`](ur-012.md)
- **Implementer:** `human_scratch_implementer`
- **Independent gate:** DISTINCT `human_scratch_reviewer`, never the implementer
- **Allowed implementer write:** product + tests + `evidence/P33/implementation-<NN>.md`
- **Allowed reviewer write:** `reviews/P33-review-<NN>.md` only
- **Ledger/marker writes:** root coordinator only

## Contract

Implement `UR-012` exactly as its frozen source requires; see [`ur-012.md`](ur-012.md). Rule from
the frozen text.

Repo hard rules: no `as`, no `any`, no `!` in product code; functional and immutable; tests
mandatory; never Playwright `--debug`/`--ui`/`--headed`/`show`; no parentheses in commit messages.

All six checks pass before handback, E2E as FULL-SUITE `--retries=0`, at least 3 consecutive runs
with a per-run digest EXCLUDING `next-env.d.ts`, which `next dev` rewrites on every start. Exactly
ONE E2E campaign runs repo-wide because `playwright.config.ts` pins :3000 with no env override: use
your own git worktree and ask root to sequence the port. Never `CI=true` for the runs — it yields 1
worker and 2 retries, inverting the required profile.

Secret-safety is blocking. Never commit the principal's real financial data; use synthetic fixtures.

## Completion

`UR-012` is markerless and immutable. It passes only when P33 passes independent review.
