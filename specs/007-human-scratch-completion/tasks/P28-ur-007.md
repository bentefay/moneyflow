# P28 — UR-007

- **Status:** queued
- **Requirement:** `UR-007` (frozen source `specs/010-user-reported-refinements-2/spec.md`,
  markerless)
- **Requirement task:** [`ur-007.md`](ur-007.md)
- **Implementer:** `human_scratch_implementer`
- **Independent gate:** DISTINCT `human_scratch_reviewer`, never the implementer
- **Allowed implementer write:** product + tests + `evidence/P28/implementation-<NN>.md`
- **Allowed reviewer write:** `reviews/P28-review-<NN>.md` only
- **Ledger/marker writes:** root coordinator only

## Contract

Implement UR-007 exactly as its frozen source requires; see [`ur-007.md`](ur-007.md). Rule from the
frozen text, not from orientation notes.

Repo hard rules: no `as`, no `any`, no `!` in product code; functional and immutable; money as
integer minor units; tests mandatory; never Playwright `--debug`/`--ui`/`--headed`/`show`; no
parentheses in commit messages.

All six checks pass before handback:
`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm test:e2e`, E2E validated with
FULL-SUITE `--retries=0` runs. NOTE: Next 16's dev lock is distDir-scoped and a human dev server may
hold the main checkout; run E2E in an isolated git worktree and never kill that server.

Secret-safety is blocking: no key material, seed phrase, recovery material, `SUPABASE_JWT_SECRET`
value, presence key, invite fragment or vault plaintext in code, tests or evidence. Never commit the
principal's real financial data; use synthetic fixtures.

## Completion

UR-007 is markerless and immutable. It passes only when P28 passes independent review, recorded in
the ledgers by root.
