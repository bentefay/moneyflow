# UR-009 — Automations conformance re-verification

- **Status:** queued
- **Source:** `specs/011-automations-conformance/spec.md` lines 16-61 (frozen, markerless)
- **Owning package:** P30
- **Re-verifies:** `HS-007` / `P17A`-`P17D`, which remain `passed` and are NOT reopened

## The reported defect, and root's read of the code

The principal added a tag and changed a description on an imported transaction and NO controls
appeared, so no rule could be created or applied to other transactions.

Root's read, to be verified independently rather than trusted:

- `computeFieldRuleRobotState` (`src/components/features/transactions/field-rule-robot-state.ts`)
  returns `{kind: "none"}` whenever `selectWinningRule` finds no existing rule. `page.tsx:486-505`
  renders `TransactionRuleRobot` only when that state is NOT `none`. This is CORRECT for the robot:
  the frozen text says the robot surfaces a rule that already exists.
- The frozen text ALSO requires a SECOND, DISTINCT surface: `human-scratch.md:249-252` says that
  when you apply a description alias "where the description text doesn't already match a rule, some
  controls appear" — the four-choice select, tick button, "only if $x" and "only this account".
  `:289-292` then says description, tags and person percentage rules "work similarly", with an extra
  "add tags" / "set tags" select for tags.
- Root found NO component implementing those creation controls: a repo-wide search for a proposal or
  rule-creation surface under `src/components/features/transactions/` returned nothing, and
  `InlineEditableTags.tsx` contains no robot or proposal wiring at all.

So the likely gap is that the DRIFT/existing-rule surface shipped while the CREATION surface did
not, for every field. Verify this yourself before fixing; do not assume root's read is complete.

## What to do

1. **Audit first, clause by clause.** Walk `specs/human-scratch.md:248-295` and record, for each
   clause, whether the shipped behaviour conforms, citing the code and the test that proves it. A
   clause with no automated test covering it is a GAP to close, not a pass. Produce this table
   before writing any fix.
2. **Fix every divergence found**, with a test that fails without the fix.
3. Pay particular attention to: the creation controls existing at all; being offered for
   description, tags AND allocation; the tags-only "add tags" / "set tags" select; the allocation
   rule spanning all person columns; last choices remembered in vault user preferences; the four
   select semantics where "Updating" auto-applies on blur and "Update" needs the tick; precedence
   description < description+amount < description+account < description+account+amount; description
   rules NOT applying to manually created transactions while tag and allocation rules DO.
4. Reuse the automations-page editor UI as the frozen text requires; do not build a parallel one.

## Constraints

Existing passed behaviour must not regress: `field-rule-parity.spec.ts` and the P17A-D suites stay
green. This package may add the missing surface and fix divergences; it may NOT redesign the rule
engine or change rule storage.

## Tests

E2E reproducing the principal's exact report: import a transaction, change its tag, and assert the
creation controls appear and can create a rule that applies to the other matching transactions. Same
for a description alias change and for a person percentage change. Unit coverage for precedence and
for the add/set tag semantics.
