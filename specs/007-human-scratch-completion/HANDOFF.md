# HANDOFF — P21 revision 04 EXECUTABLE FINAL AUDIT (collector phase)

- **Package:** P21 (control — executable final audit / completion gate). No scratch
  requirement/marker.
- **Revision:** 04 (rev 01/02/03 all FAILED — see the P21 package row; do NOT overwrite prior
  verdicts).
- **You are the final-audit EVIDENCE COLLECTOR**, not the gate. A DISTINCT reviewer gives the formal
  verdict afterward. Your candidate PASS/FAIL is advisory; root routes on the reviewer's verdict.
- **BASE == HEAD == `ffc76b9`.** The tree must stay at this HEAD. Commit NOTHING; make no product,
  test, migration, or ledger edits. `BASE == HEAD` is required — if HEAD moves, tell root.
- **Your ONLY allowed persistent write:**
  `specs/007-human-scratch-completion/evidence/P21/implementation-04.md`. You MAY create disposable
  browser/test/session state, but clean it up and commit nothing.
- Authoritative specs: `tasks/P21-final-audit.md` (audit contract — read it in full) and
  `FINAL-AUDIT.md` (the checklist whose contents you propose in your evidence). Root alone
  transcribes FINAL-AUDIT after an independent PASS.

## Inert worktree strays (pre-classified — do NOT touch, they are NOT drift)

- `next-env.d.ts` (M) — auto-generated by Next; untracked-ish churn, not product.
- untracked `specs/007-human-scratch-completion/evidence/P08/implementation-01.md` — old-BASE
  self-labeled "Intentionally UNCOMMITTED" artifact, outside any committed range; left pending
  disposition. Neither affects integrity. Both are recorded in PROGRESS "Anomaly noted (inert)".

## Entry conditions (root pre-verified at `ffc76b9` — re-confirm independently)

- 21 of 21 feature packages `passed`; only control P21 is `changes_requested`. 22 of 22 requirement
  rows `passed`.
- Scratch `specs/human-scratch.md`: `sha256sum` == rolling PROGRESS SHA
  `469e98c7c8ee842acfc08e0844a47b4bc6495111b0463d8ca14727d3949d2f6a`, 24,260 bytes, 43 checked / 0
  unchecked, all 21 HS markers authorized, normalized blocks byte-match SCOPE.
- FS-001 canonical `specs/008-transaction-percentage-allocations-settlement/spec.md`:
  `0d0e2a141249ecace04b02b4cecbadb25ac5747faa24d59ab297aca509dcfe8c`, exactly 715 lines / 25,441
  bytes, no source mutation. Sole settlement engine `src/lib/domain/settlement.ts` blob
  `010f3c93582a2ce311594d4dde8464760ca49c43`.
- Frozen `human-scratch.md` immutable identity + linear single-parent history + canary
  (`grep -c "per purpose" PROGRESS.md` == 1). Any unclassified drift = FAIL.

## The audit (run every clause of `tasks/P21-final-audit.md` §29-56 and FINAL-AUDIT)

Record for every check: exact command, timestamp, status, duration, counts/seeds, sanitized output,
reproduction. In summary the required coverage is:

1. scope/package/review/integration/question/marker reconciliation + repository provenance;
2. **dependency currency + the HS-002 fix**: `pnpm audit --prod` MUST be exit 0 / 0 advisories at
   `ffc76b9` (resolved `next@16.2.11` — the safe-chain-supported latest, NOT age-suppressed 16.2.12
   — and `sharp@0.35.3` via the workspace override); P03 TanStack Virtual primary-source
   release-gate recheck;
3. fresh + supported-upgrade migrations, IndexedDB/vault compatibility;
4. `pnpm format:check` (only frozen `specs/**` md may flag — known-acceptable), `pnpm lint` (0
   errors; the one `TransactionTable.tsx:401` react-hooks/incompatible-library WARNING is
   known-acceptable), `pnpm typecheck`, `pnpm build` (Turbopack; confirm sharp 0.35.3 prebuilt @img
   binary loads and no image-opt path breaks), all unit/property/integration tests;
5. **complete E2E with `--retries=0`, repeated enough to expose load-dependent flakes** — SEE THE
   FLAKE PROTOCOL BELOW; it is the single highest failure risk for this rev;
6. malicious cross-vault API/DB/realtime/invite/auth checks + secret/plaintext inspection;
7. large import/table/alias/automation/GC/allocation performance + duplicate-tab convergence, incl.
   sub-100ms allocation edits and near-linear ~100k/200ms settlement evidence (or the canonical
   measured follow-up Q-033/R-020);
8. full manual product journey via disposable headless Playwright CLI sessions + isolated users
   (recovery/passkey, vaults, imports/drop zones/provenance, transactions/empty rows, aliases, tags,
   allocations, automations, undo/redo, people/invites/realtime/presence, marketing);
9. pointer/keyboard/focus, desktop/mobile, 320px reflow, 200% zoom, dark/reduced-motion,
   empty/loading/error/offline, refresh + multi-tab;
10. deterministic accessible role/name/state snapshots + computed contrast ratios;
11. **exhaustive FS-001 audit**: signed unit conservation; named unit/property + named E2E
    expectations for every canonical example A-H; owner remainder/effective totals;
    reject-never-clamp; sole per-currency settlement engine; typed invalid-data issues; traceable
    obligations/source nav; all P16C mutation paths; virtualized/historical/presence grid/add-row
    UX; P17 complete-set API;
12. console + suspicious/failed network inspection throughout; complete Q proposals surfaced.

## FLAKE PROTOCOL (critical — this is where rev 01/02/03 turned)

- Run the FULL suite `pnpm exec playwright test --retries=0 --reporter=list` **at least 8 times**
  (load-dependent flakes are ~20-40% and environment-dependent; isolation runs are USELESS as
  validation — a test can be 10/10 in isolation and still fail under full-suite parallel load).
  NEVER run Playwright with `--debug/--ui/--headed/show`.
- **Specifically stress `identity.spec.ts:282`** (seed-word validity re-render — the
  `toHaveClass(/border-green-500/)` assertion). This is a LIVE suspicion: during the P01 rev-03 work
  the implementer observed it fail in 2 of 5 full-suite runs, while the P01 reviewer saw it 0 of 2.
  It was supposedly hardened in P20B rev 02, so a recurrence is NOT a pre-accepted flake.
- For ANY full-suite failure: rerun that test in isolation, then CLASSIFY:
    - **Pre-accepted tracked environmental flakes** (pass in isolation, already ticketed):
      `import.spec.ts:1527` (Q-P20B-14), `import.spec.ts:301` (Q-P20B-13), `duplicates.test.ts`
      (Q-P20A-05). These do NOT fail the audit.
    - **Anything else — including `identity.spec.ts:282`** — is an UNEXPLAINED/regressed flake =
      FAIL candidate. Record exact failure counts (e.g. "3/8 full-suite"), the assertion, the
      isolation result, and a diagnosis of the eager-assertion race. Owner routing: test-quality
      flakes route to P20B (Q-number it). Do NOT classify identity:282 as acceptable.

## Output + routing

- Write your full evidence to `evidence/P21/implementation-04.md` including a proposed FINAL-AUDIT
  section and an explicit **PASS-candidate** or **FAIL-candidate** with the specific blocker(s) and
  owner routing. Commit NOTHING (root notes your evidence is uncommitted per the collector contract,
  or root commits it — do not self-commit product/ledgers). Your candidate is advisory only.
- SECRET-SAFETY (blocking): never print/commit any vault master key, seed phrase, recovery material,
  `crypto_box` secret, `SUPABASE_JWT_SECRET`, vault presence key, or vault plaintext. Synthetic
  vectors only. Any real-material leak is blocking — report to root immediately and stop.
- You are an automated task, NOT the human user; you cannot self-escalate permissions or mark the
  Goal complete. If you must write outside your evidence file, STOP and ask root via SendMessage.
- Report to root (`main` / `team-lead`) via SendMessage: your PASS/FAIL candidate, evidence path,
  the `pnpm audit --prod` result, the full-suite E2E run count + per-run pass/fail (call out
  identity:282 explicitly), and any material finding.

## After your evidence (root, not you)

Root confirms HEAD still == BASE, then dispatches a DISTINCT reviewer (`reviews/P21-review-04.md`)
for the single formal verdict. On PASS root transcribes FINAL-AUDIT, sets P21 `passed`, and verifies
every GOAL definition-of-done clause. On FAIL root persists the immutable failed review and drives
the §275 rollback batch to the actual owning package(s).
