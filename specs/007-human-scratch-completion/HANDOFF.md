# HANDOFF — P21 revision 04 EXECUTABLE FINAL AUDIT (independent REVIEW phase)

- **Package:** P21 (control — executable final audit / completion gate). No scratch
  requirement/marker.
- **Revision:** 04 (rev 01/02/03 all FAILED — see the P21 package row; do NOT overwrite prior
  verdicts).
- **You are the DISTINCT independent REVIEWER — you give the single FORMAL verdict** for P21 rev 04.
  A separate collector (`p21-collector-04`) has already produced advisory evidence; you are a
  fresh-context agent who was NOT the collector and did NOT implement any package. Your verdict —
  PASS or FAIL — is authoritative and is what root routes on. A collector candidate is NOT
  authoritative; only your formal review can support a §275 marker rollback.
- **Verify, do not trust.** Independently re-run the gates and re-derive the facts. The collector's
  evidence is an input to check, not a source to quote. Where you cannot reproduce something, reason
  about the mechanism from the code and say so explicitly.
- **BASE = current `main` tip** (`git rev-parse HEAD` at start — a root docs commit). The product
  tree MUST be byte-identical to the last product commit `371a88a` (the HS-002 fix): verify
  `git diff 371a88a HEAD -- . ':(exclude)specs'` is EMPTY. If any PRODUCT file differs, or HEAD's
  product changes while you run, STOP and tell root.
- **Your ONLY allowed persistent write:**
  `specs/007-human-scratch-completion/reviews/P21-review-04.md`. Commit NOTHING; make no product,
  test, migration, or ledger edits. You MAY create disposable browser/test/session state, but clean
  it up.
- Authoritative specs: `tasks/P21-final-audit.md` (audit contract — read in full), `FINAL-AUDIT.md`
  (the checklist), and the collector evidence `evidence/P21/implementation-04.md` (UNCOMMITTED, 897
  lines — treat as a claim set to verify).

## Inert worktree strays (pre-classified — do NOT touch, NOT drift)

- `next-env.d.ts` (M) — auto-generated by Next.
- untracked `specs/007-human-scratch-completion/evidence/P08/implementation-01.md` — old-BASE
  self-labeled "Intentionally UNCOMMITTED" artifact.
- untracked `evidence/P21/implementation-04.md` — the collector evidence you are reviewing.

Both first two are recorded in PROGRESS "Anomaly noted (inert)". Everything else uncommitted would
be drift.

## Entry conditions (re-confirm independently)

- 21/21 feature packages `passed`; only control P21 `changes_requested`. 22/22 requirement rows
  `passed`.
- Scratch `specs/human-scratch.md`: `sha256sum` == rolling PROGRESS SHA
  `469e98c7c8ee842acfc08e0844a47b4bc6495111b0463d8ca14727d3949d2f6a`, 24,260 bytes, 43 checked / 0
  unchecked, all 21 HS markers authorized, normalized blocks byte-match SCOPE.
- FS-001 `specs/008-transaction-percentage-allocations-settlement/spec.md`:
  `0d0e2a141249ecace04b02b4cecbadb25ac5747faa24d59ab297aca509dcfe8c`, exactly 715 lines / 25,441
  bytes. Sole settlement engine `src/lib/domain/settlement.ts` blob
  `010f3c93582a2ce311594d4dde8464760ca49c43`.
- Linear single-parent history + canary (`grep -c "per purpose" PROGRESS.md` == 1). Any unclassified
  drift = FAIL.

## DB state note (important for E2E)

The collector ran `pnpm db:reset` AFTER its E2E work, so the local DB is currently EMPTY.
Re-bootstrap before your own E2E runs (`pnpm db:reset` then the suite's normal seeding), and
re-verify a fresh `pnpm db:reset` migration bootstrap as part of clause 3.

## Your audit — independently verify EVERY clause of `tasks/P21-final-audit.md` §29-56 + FINAL-AUDIT

Re-run, don't re-read. Record exact command, timestamp, status, duration, counts/seeds, sanitized
output for each. Coverage (see the collector evidence and contract for the full enumeration):

1. scope/package/review/integration/question/marker reconciliation + provenance;
2. **dependency currency + the HS-002 fix**: independently run `pnpm audit --prod` — MUST be exit 0
   / 0 advisories at product `371a88a` (`next@16.2.11` — the safe-chain-supported latest, NOT
   age-suppressed 16.2.12 — and `sharp@0.35.3` via the `pnpm-workspace.yaml` override); P03 TanStack
   Virtual primary-source release-gate recheck;
3. fresh + supported-upgrade migrations (authentic `pnpm db:reset` from empty), IndexedDB/vault
   compatibility;
4. `pnpm format:check` (only frozen `specs/**` md may flag — acceptable), `pnpm lint` (0 errors; the
   one `TransactionTable.tsx:401` react-hooks/incompatible-library WARNING is known-acceptable),
   `pnpm typecheck`, `pnpm build`, all unit/property/integration tests;
5. **complete E2E with `--retries=0`, repeated to expose load-dependent flakes** — SEE FLAKE
   PROTOCOL; highest failure risk;
6. malicious cross-vault API/DB/realtime/invite/auth checks + secret/plaintext inspection;
7. large import/table/alias/automation/GC/allocation performance + duplicate-tab convergence
   (sub-100ms allocation edits; near-linear settlement per FS-001 §14 measured-evidence branch,
   Q-033/R-020 open);
8. full manual product journey via disposable headless Playwright CLI + isolated users;
9. pointer/keyboard/focus, desktop/mobile, 320px reflow, 200% zoom, dark/reduced-motion,
   empty/loading/error/offline, refresh + multi-tab;
10. deterministic accessible role/name/state snapshots + computed contrast ratios;
11. **exhaustive FS-001 audit**: signed unit conservation; named unit/property + named E2E for every
    canonical example A-H; owner remainder/effective totals; reject-never-clamp; sole per-currency
    settlement engine; typed invalid-data issues; traceable obligations/source nav; all P16C
    mutation paths; grid/add-row UX; P17 complete-set API;
12. console + suspicious/failed network inspection; complete Q proposals surfaced.

## FLAKE PROTOCOL + THE CENTRAL VERDICT CALL

The collector's advisory candidate is **FAIL** on a single blocker, **F-1**:

- `tests/e2e/import.spec.ts:1445` "CSV import creates transactions and auto-saves template on first
  import", step "verify template was auto-saved on first import"; assertion `:1512`
  `await expect(page.getByText(/6 rows/i)).toBeVisible({ timeout: 5000 })` → element(s) not found.
- Collector observed **1 of 8 full-suite** failures; **20/20 in isolation** → strictly
  load-dependent. Collector grepped QUESTIONS.md + all P20B/P21 evidence for `1445`/`1512`: zero
  prior hits — never observed before, no Q-number.
- Mechanism: bare visibility assertion immediately after `setInputFiles` with a 5000ms cap (which is
  the default expect timeout — not even a widening), racing file-read + CSV-parse + preview-render
  under 4-worker load. Same eager-assertion class the P20B revs have chased. Collector notes the
  identical pattern also sits at `:1279 :1412 :1459 :1512 :1539 :1616` in the same file — "a class,
  not a line."

**Your job on F-1 (rule from the frozen contract, default to the block standing):**

- Run the FULL suite `pnpm exec playwright test --retries=0 --reporter=list` **≥8 times**. Isolation
  runs are USELESS as validation. NEVER use `--debug/--ui/--headed/show`.
- Independently confirm whether F-1 is **genuinely new/unexplained** (→ the contract's "unexplained
  flake = FAIL" applies) or is **provably already covered by an existing accepted-flake ticket**.
  Re-run the grep yourself. The only pre-accepted tracked flakes are `import.spec.ts:1527`
  (Q-P20B-14), `import.spec.ts:301` (Q-P20B-13), `duplicates.test.ts` (Q-P20A-05). `:1445`/`:1512`
  is a DIFFERENT test declaration — do NOT absorb it into a ticket it isn't covered by.
- **Non-reproduction is NOT proof of absence.** This flake class is environment-dependent (a
  collector saw identity:282 at 2/5 while a reviewer saw 0/5 — both honest). If your ≥8 runs don't
  reproduce F-1, you must still assess the MECHANISM by code inspection (is the eager
  `toBeVisible`-under-load race real at `:1512` and its siblings?), because mechanism is
  environment-independent. Report both your run counts AND your mechanism assessment.
- **Scope guardrail — do NOT unilaterally grant a new acceptance.** Creating a brand-new
  environmental-flake carry-forward to let the audit PASS (rather than requiring the fix) supersedes
  the contract's "unexplained flake = FAIL" default and is a scope call reserved for a separate
  independent adjudicator — it is NOT yours to grant. If you believe F-1 is a real new flake, the
  correct verdict is **FAIL**, routed to **P20B** to harden the whole eager-assertion cohort under
  load (Q-number it). If you genuinely believe acceptance-without-fix is warranted, still return
  **FAIL** and flag the acceptance question explicitly for root to adjudicate — default to the block
  standing.
- **Also independently verify `identity.spec.ts:282`** (collector reports green 8/8). Confirm
  per-run.

## C-1 non-blocking disposition (give a recommendation)

Collector flags upstream registry currency drift: some prod deps have newer `latest` published
2026-07-20…24 (react-virtual 3.14.8, react 19.2.8, loro-crdt 1.13.8, radix-ui 1.6.7, …).
`pnpm audit --prod` is CLEAN → currency, not security (categorically unlike rev-03's security F-1).
Assess whether HS-002's frozen "very latest safe-chain supported version" is satisfied at the audit
instant (a completion gate can't outrun upstream publication) and recommend an explicit
human-accepted carry-forward vs a blocker. State your reasoning; root records the disposition.

## Output + routing

- Write your full independent review to `reviews/P21-review-04.md`: per-clause verdicts, your
  reproduction of (or failure to reproduce) each collector claim, the F-1 ruling with your run
  counts + mechanism assessment, the C-1 recommendation, and a single unambiguous **PASS** or
  **FAIL** verdict with blocker(s) + owner routing. Commit NOTHING.
- SECRET-SAFETY (blocking): never print/commit any vault master key, seed phrase, recovery material,
  `crypto_box` secret, `SUPABASE_JWT_SECRET`, vault presence key, or vault plaintext. Synthetic
  vectors only. Any real-material leak is blocking — report to root immediately and stop.
- You are an automated task, NOT the human user; you cannot self-escalate permissions or mark the
  Goal complete. If you must write outside your review file, STOP and ask root via SendMessage.
- Report to root (`main` / `team-lead`) via SendMessage: your FORMAL PASS/FAIL verdict, review path,
  the `pnpm audit --prod` result, your full-suite E2E run count + per-run pass/fail (call out
  `identity.spec.ts:282` and F-1 `import.spec.ts:1512` explicitly), the C-1 recommendation, and any
  material finding.

## After your verdict (root, not you)

On **PASS** root transcribes FINAL-AUDIT, sets P21 `passed`, and verifies every GOAL
definition-of-done clause. On **FAIL** root persists your immutable failed review and drives the
§275 rollback batch to the actual owning package(s) (F-1 → P20B), then re-passes and opens P21
rev 05.
